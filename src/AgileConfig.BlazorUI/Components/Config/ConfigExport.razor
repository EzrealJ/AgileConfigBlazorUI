@using AgileConfig.BlazorUI.Helpers
<Modal Visible=@Visible Width="@("61.8%")"
       Title=@($"导出前预览,当前环境为{Para?.ENV}")
       OnCancel=@Cancel
       OnOk=@SaveAsync>
    <TextArea AutoSize @bind-value="_value" OnBlur=@(e=>JsonDataHelper.SerializeJson(ref _value)) />

</Modal>